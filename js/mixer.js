var Mixer = function () {
	// constructor
	this.current = null;
	this.next = null;
	
	var audio = null; // anything...
	
	this.prepare = function ( track ) {
		// load track into the deck2
	};
	
	this.mix = function () {
		// skips to the deck2
	};
};